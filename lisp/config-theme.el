(use-package nano-theme
  :disabled t ;; we hardcode too much sol-faces to turn this on for now
  :straight t
  :custom
  (nano-window-divider-show t))
  ;; TODO (pangt): map colors to solarized

(provide 'config-theme)
