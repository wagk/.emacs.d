;; TODO: implement a org export backend to elisp
;; refer to `org-element-all-elements' for all the elements we need to
;; translate.

(require 'ox)

(org-export-define-backend 'elisp
  ;; TODO: insert alist here
  '())
